spring:
  application:
    name: client-rest-service
  cloud:
    config:
      name: ${spring.application.name}
      uri: http://192.168.99.100:8888/config-server
      enabled: true
      failFast: true
#management.endpoints.web.exposure.include=refresh
